class Solution:
    def buildArray(self, nums: List[int]) -> List[int]:
        arr= []
        for num in nums:
            arr.append(nums[num])
        return arr
            
            
        