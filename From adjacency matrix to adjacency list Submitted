n = int(input())

for ind in range(1,n+1):
    row = input().split()
    temp = []
    
    for i in range(len(row)):
        if row[i] == "1":
            temp.append(str(i + 1))
    ans = " ".join(temp)
    print(str(len(temp)) + " " + ans)
